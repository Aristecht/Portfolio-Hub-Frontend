mutation CreateDonationTier($data: CreateDonationTierInput!) {
  createDonationTier(data: $data) {
    id
    title
    description
    amount
    currency
    benefits
    isActive
    position
  }
}
