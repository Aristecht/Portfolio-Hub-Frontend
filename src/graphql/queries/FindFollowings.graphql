query findFollowings($data: FindFollowings!) {
  findFollowings(data: $data) {
    data {
      following {
        id
        avatar
        username
      }
      followingId
    }
    meta {
      limit
      page
      total
      totalPages
    }
  }
}
