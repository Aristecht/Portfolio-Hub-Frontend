query GetPayoutHistory($data: HistoryPaymentInput) {
  getPayoutHistory(data: $data) {
    data {
      id
      amount
      method
      status
      createdAt
      processedAt
      failureReason
      transactionId
    }
    meta {
      page
      limit
      total
      totalPages
    }
  }
}
